# JavaScript action
name: setup-jira

description: Set up Jira CLI

inputs:
  version:
    description: Version of the CLI to use
    required: false
    default: 1.0.20

runs:
  using: 'node12'
  main: './dist/index.js'